{
  "id": "barrel-reexport-change",
  "category": "breaking",
  "description": "A symbol is removed from a barrel/index.ts re-export, breaking downstream packages",
  "fixture": "standalone",
  "input": {
    "scope": "changed"
  },
  "expected": {
    "risk": "high",
    "semver": "major",
    "explanation_contains": ["reexport", "removed"]
  },
  "notes": "Barrel re-export removals are the most common accidental breaking change in monorepos."
}
