{
  "id": "shared-type-regression",
  "category": "type-change",
  "description": "A shared function gains a required parameter, causing TS2554 at the call site",
  "fixture": "standalone",
  "input": {
    "file_path": "src/ItemList.ts",
    "diagnostic_code": "TS2554"
  },
  "expected": {
    "topCandidateFile": "src/types.ts",
    "explanation_contains": ["definition-site", "changed"],
    "confidence_floor": "medium"
  },
  "notes": "renderItem in types.ts gained a required options parameter — call site in ItemList.ts gets TS2554. Definition traces to types.ts."
}
