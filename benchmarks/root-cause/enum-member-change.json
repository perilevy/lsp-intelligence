{
  "id": "enum-member-change",
  "category": "enum-change",
  "description": "An enum member is removed and a function gains a required parameter, causing TS2554 at call sites",
  "fixture": "standalone",
  "input": {
    "file_path": "src/handler.ts",
    "diagnostic_code": "TS2554"
  },
  "expected": {
    "topCandidateFile": "src/types.ts",
    "explanation_contains": ["definition-site", "changed"],
    "confidence_floor": "medium"
  },
  "notes": "processStatus now requires a second argument — handler.ts calls with 1 arg get TS2554. Root cause is types.ts."
}
