{
  "mcpServers": {
    "lsp": {
      "command": "/bin/sh",
      "args": ["-c", "WORKSPACE=\"$PWD\" && cd \"${CLAUDE_PLUGIN_ROOT}\" && test -d node_modules || npm install --registry https://registry.npmjs.org --omit=dev >&2 && LSP_WORKSPACE_ROOT=\"$WORKSPACE\" exec node dist/index.js"]
    }
  }
}
