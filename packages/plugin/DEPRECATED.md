# Deprecated

The assets in this package (skills, hooks, CLAUDE.md, launch scripts) have been
merged into the main `lsp-intelligence` npm package as of 0.3.1.

The plugin is now distributed as a single package through the Claude Code
marketplace. See `marketplace/marketplace.json` at the repo root.

This directory will be removed in a future cleanup.
