{
  "id": "added-optional-param",
  "category": "non-breaking",
  "description": "An optional parameter is added to an exported function signature",
  "fixture": "monorepo",
  "input": {
    "scope": "changed"
  },
  "expected": {
    "risk": "low",
    "semver": "minor",
    "explanation_contains": ["optional", "parameter", "added"]
  },
  "notes": "Adding optional params is non-breaking — existing callers don't need changes."
}
