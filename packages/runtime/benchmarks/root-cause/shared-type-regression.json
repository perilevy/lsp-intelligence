{
  "id": "shared-type-regression",
  "category": "type-change",
  "description": "A shared type in packages/types is changed (field removed), causing downstream errors in packages/app",
  "fixture": "monorepo",
  "input": {
    "file_path": "packages/app/src/ItemList.ts",
    "diagnostic_code": "TS2339"
  },
  "expected": {
    "topCandidateFile": "packages/types/src/index.ts",
    "explanation_contains": ["type", "changed", "removed"],
    "confidence_floor": "medium"
  },
  "notes": "Classic monorepo regression: upstream type change breaks downstream consumer."
}
